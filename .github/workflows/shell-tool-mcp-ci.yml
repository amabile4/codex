name: shell-tool-mcp-ci

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled in fork
        run: echo "shell-tool-mcp-ci workflow is disabled in this fork"